version: "3"


services:
    nginx:
      image:
        "nginx:latest"
      ports:
        - "80:80"
      volumes:
        - ./config/nginx:/etc/nginx/conf.d
        - ./:/work
      command: ["nginx", "-g", "daemon off;"]
